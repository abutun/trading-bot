-r requirements.txt
-r requirements-migration.txt

# Test, lint, coverage, and dependency-audit tooling used locally and in CI.
pytest>=8.3.0,<9.0.0
pytest-cov>=5.0.0,<7.0.0
ruff>=0.8.0,<1.0.0
pip-audit>=2.8.0,<3.0.0
